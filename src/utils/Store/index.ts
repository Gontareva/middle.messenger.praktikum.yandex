export { IBlockProps } from './types';

import Store, {
	dispatch,
	makeSelector,
	attachListener,
	detachListener,
	createStore,
	getState
} from './Store';

export {
	dispatch,
	makeSelector,
	attachListener,
	detachListener,
	createStore,
	getState
};
export default Store;
