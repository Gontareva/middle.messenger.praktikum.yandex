mixin formInput(options={})
    .form-input(class=options.classes)
        .form-input__title #{options.title}
        input.form-input__value(
            type=options.type || 'text',
            placeholder=options.value,
            value=options.value,
            readonly=options.readOnly,
            name=options.name
        )